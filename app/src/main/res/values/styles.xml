<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="android:windowBackground">@color/color_window_background</item>
    </style>

    <style name="AppTheme.DarkMatter" parent="ThemeOverlay.AppCompat.Dark"/>

    <style name="AppTheme.DarkMatter.Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

</resources>
